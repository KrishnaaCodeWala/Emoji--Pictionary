// TODO (Track B). Scanline flood fill on ImageData. Mutates and returns `img`.
export function floodFill(img: ImageData, _x: number, _y: number, _rgba: [number, number, number, number], _tolerance = 32): ImageData {
  return img;
}
