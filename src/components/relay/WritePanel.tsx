'use client';
// TODO (Track C)
export interface WritePanelProps {
  onSubmit: (phrase: string) => void;
  submitted: boolean;
  onInspire?: () => string;
}
export default function WritePanel(_p: WritePanelProps) { return null; }
