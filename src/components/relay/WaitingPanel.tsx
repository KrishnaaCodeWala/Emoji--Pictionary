// TODO (Track C)
import type { RelayProgress } from '@/lib/types';
export interface WaitingPanelProps { progress: RelayProgress | null; phaseLabel: string }
export default function WaitingPanel(_p: WaitingPanelProps) { return null; }
