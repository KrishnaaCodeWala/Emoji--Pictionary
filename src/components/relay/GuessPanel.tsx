'use client';
// TODO (Track C)
export interface GuessPanelProps {
  canvas: string;
  onSubmit: (guess: string) => void;
  submitted: boolean;
}
export default function GuessPanel(_p: GuessPanelProps) { return null; }
