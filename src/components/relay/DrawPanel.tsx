'use client';
// TODO (Track C)
export interface DrawPanelProps {
  phrase: string;
  onSubmit: (emojis: string) => void;
  submitted: boolean;
}
export default function DrawPanel(_p: DrawPanelProps) { return null; }
