// TODO (Track C)
import type { AlbumStep } from '@/lib/types';
export interface AlbumCardProps { step: AlbumStep; isNew?: boolean }
export default function AlbumCard(_p: AlbumCardProps) { return null; }
