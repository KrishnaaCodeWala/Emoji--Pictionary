'use client';
// TODO (Track D)
import type { Player } from '@/lib/types';
export interface ResultsProps { players: Player[]; isHost: boolean; onPlayAgain: () => void }
export default function Results(_p: ResultsProps) { return null; }
