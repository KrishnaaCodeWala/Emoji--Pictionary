'use client';
// TODO (Track C)
import type { InputMode } from '@/lib/types';
export interface InputPickerProps { input: InputMode; editable: boolean; onChange: (input: InputMode) => void }
export default function InputPicker(_p: InputPickerProps) { return null; }
