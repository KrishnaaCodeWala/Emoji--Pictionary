'use client';
// TODO (Track C)
import type { Message, Player } from '@/lib/types';
export interface ChatProps { messages: Message[]; players: Player[] }
export default function Chat(_p: ChatProps) { return null; }
