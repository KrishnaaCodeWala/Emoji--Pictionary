'use client';
// TODO (Track C)
export interface EmojiPickerProps { value: string; onChange: (emojis: string) => void; disabled?: boolean }
export default function EmojiPicker(_p: EmojiPickerProps) { return null; }
