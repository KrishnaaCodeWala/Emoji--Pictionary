'use client';
// TODO (Track C)
import type { RevealPayload } from '@/lib/types';
export interface RevealCardProps { reveal: RevealPayload | null }
export default function RevealCard(_p: RevealCardProps) { return null; }
