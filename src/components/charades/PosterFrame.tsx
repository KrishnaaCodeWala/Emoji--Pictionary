'use client';
// TODO (Track C)
export interface PosterFrameProps { url: string | null; title?: string; blurred?: boolean; className?: string }
export default function PosterFrame(_p: PosterFrameProps) { return null; }
