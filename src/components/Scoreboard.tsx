// TODO (Track D)
import type { Player } from '@/lib/types';
export interface ScoreboardProps { players: Player[]; currentDrawerId?: string | null; meId?: string | null }
export default function Scoreboard(_p: ScoreboardProps) { return null; }
