'use client';
// TODO (Track D). One animated panel per mode/theme.
import type { GameMode } from '@/lib/types';
export interface ThemeShowcaseProps { mode: GameMode; active: boolean }
export default function ThemeShowcase(_p: ThemeShowcaseProps) { return null; }
